- wrap_layout :layout do
  .container 
    %section
      %article
        %header
          %h2= current_article.title
          %time{ :datetime => current_article.date}= current_article.date.strftime('%b %e') 
        = yield