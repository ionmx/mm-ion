---
title: Blog
pageable: true
per_page: 10
---

.container 
  %section
    - page_articles.each_with_index do |article, i|
      %article
        %header
          %h2= link_to article.title, article
          %time{ :datetime => article.date}= article.date.strftime('%b %e') 
        
        = article.body


  - if paginate
    - if num_pages > 1
      %p "Página #{page_number} de {num_pages}
    - if prev_page
      %p= link_to 'Anterior', prev_page
    - if next_page
      %p= link_to 'Siguiente', next_page